# ── Core ──────────────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── LangChain ─────────────────────────────────────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0

# Instalează doar provider-ul pe care îl folosești:
langchain-openai>=0.2.0           # pentru OpenAI / Azure OpenAI
# langchain-anthropic>=0.2.0      # pentru Anthropic Claude
# langchain-google-vertexai>=2.0  # pentru Google Vertex AI (recomandat ZDR)
langchain-google-genai>=1.0.0

# ── Cache ─────────────────────────────────────────────────────────────────────
redis[asyncio]>=5.0.0

# ── Chunking (opțional — fallback regex disponibil) ───────────────────────────
# tree-sitter>=0.23.0
# tree-sitter-languages>=1.10.0

# ── Baza de date (opțional — pentru audit log) ────────────────────────────────
# sqlalchemy[asyncio]>=2.0.0
# asyncpg>=0.29.0
# alembic>=1.13.0

# ── Dev / Test ────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0           # client async pentru TestClient FastAPI
python-multipart>=0.0.9 # necesar pentru file upload
